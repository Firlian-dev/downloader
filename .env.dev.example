# Development environment variables
# For running the application locally with dockerized yt-dlp service

# Telegram Bot Token (required)
# Get it from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Telegram Bot Username (optional)
TELEGRAM_BOT_USERNAME=downloader_bot

# yt-dlp execution mode - use "http" for local development with Docker
YTDLP_MODE=http

# URL of yt-dlp HTTP service running in Docker
YTDLP_SERVICE_URL=http://localhost:8090

# Directory for downloaded files (must be shared with Docker container)
DOWNLOAD_DIR=./downloads

# Cache time-to-live in hours (default: 24)
CACHE_TTL_HOURS=24

# File size limit in MB for direct upload (default: 50)
SIZE_LIMIT_MB=50
