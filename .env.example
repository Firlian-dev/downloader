# Environment variables for the Telegram Downloader Bot
# Copy this file to .env and fill in your values

# Telegram Bot Token (required)
# Get it from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Telegram Bot Username (optional)
TELEGRAM_BOT_USERNAME=downloader_bot

# Path to yt-dlp binary (default: yt-dlp)
# Only used when YTDLP_MODE=local
# On Windows: yt-dlp.exe or full path like C:\path\to\yt-dlp.exe
# On Linux/Mac: yt-dlp or full path like /usr/local/bin/yt-dlp
# On Docker: /usr/local/bin/yt-dlp
YTDLP_BIN=yt-dlp

# yt-dlp execution mode: "local" or "http"
# local - use local yt-dlp binary (default)
# http - use yt-dlp HTTP service in Docker (recommended for local development)
YTDLP_MODE=local

# URL of yt-dlp HTTP service (default: http://localhost:8090)
# Only used when YTDLP_MODE=http
YTDLP_SERVICE_URL=http://localhost:8090

# Directory for downloaded files (default: ./downloads)
# Use relative path ./downloads for Windows/Mac/Linux compatibility
# Docker will use absolute path /downloads inside container
DOWNLOAD_DIR=./downloads

# Cache time-to-live in hours (default: 24)
CACHE_TTL_HOURS=24

# File size limit in MB for direct upload (default: 50)
SIZE_LIMIT_MB=50
